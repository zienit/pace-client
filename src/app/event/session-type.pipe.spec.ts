import { SessionTypePipe } from './session-type.pipe';

describe('SessionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SessionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
